\begin{axis}[%
  width=0.95\linewidth, height=0.7\linewidth,
  every tick label/.append style={font=\scriptsize},
  % x-axis
  xlabel={number of nodes},
  xmajorgrids=true,
  xmin=51000000,
  xmax=300000000000,
  xmode = log,
  % y-axis
  ymin=0,
  ymax=2.2,
  ytick distance={0.5},
  ylabel={$\mu$s / node},
  yminorgrids=true,
  ymajorgrids=true,
  grid style={dashed,black!20},
  ]

  \only<1-> {
    \addplot+ [style=plot_buddy]
    table {./data/buddy_time_per_node.tex};
  }

  \only<3-> {
    \addplot+ [style=plot_cudd]
    table {./data/cudd_time_per_node.tex};
  }

  \only<4-> {
    \addplot+ [style=plot_sylvan]
    table {./data/sylvan_time_per_node.tex};
  }
  
  \only<5-> {
    \addplot+ [style=plot_adiar]
    table {./data/adiar_time_per_node.tex};
  }
\end{axis}